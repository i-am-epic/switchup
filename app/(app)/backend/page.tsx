import { ModulePage } from "@/components/roadmap/ModulePage";

export default function BackendPage() {
  return (
    <ModulePage
      module="backend"
      title="Backend"
      subtitle="Networking through SOLID — read, watch, practice, build, revise."
    />
  );
}
